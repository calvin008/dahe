
import type { UserConfig } from 'vite';



const viteConfig: UserConfig = {
    /* 端口号 */
    port:3888,

    /* hostname */
    hostname: 'localhost',

    /* 运行自动打开浏览器 */
    open:false,

   
    
};

export default viteConfig;